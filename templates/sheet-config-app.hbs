<section>
    <form action="">
    <ul class="flex-column gap-small reset-margin reset-padding">
		{{#each journalOptions as |option|}}
            <li>{{option}}</li>
        {{/each}}
    </ul>
    </form>
</section>